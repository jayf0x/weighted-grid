/** `fillComponent` for every case — the leftover-hole filler, styled distinctly from `Item`/`Void`
 * so a screenshot makes clear which cells the grid filled itself vs. which a case placed. */
export const Filler = () => (
  <div className="bg-fill w-full h-full" id="filler">
    _
  </div>
);
