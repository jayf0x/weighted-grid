/** Intentional-negative-space tile body. Render inside a `<GridItem>` — see `Item.tsx` for why this
 * can't wrap `GridItem` itself. */
export const Void = () => <div className="bg-void w-full h-full" id="void" />;
