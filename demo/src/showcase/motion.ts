/** The one `itemAnimation` curve every case uses for its `<Grid>`. Not a library export — it's a CSS
 * string, callers can write their own; this just avoids five copies of the same literal. */
export const FLIP_TRANSITION = '200ms cubic-bezier(0.22, 1, 0.36, 1)';
